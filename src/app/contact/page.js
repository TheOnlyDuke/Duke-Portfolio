import { Box, Typography } from "@mui/material";

export default function ContactPage({}) {
  return (
    <Box component="main">
      <Typography variant="title" >Contacts Page</Typography>
      <Typography variant="smallTitle" >Initial commit!</Typography>
    </Box>
  )
}