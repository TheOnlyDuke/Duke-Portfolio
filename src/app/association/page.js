import { Box, Typography } from "@mui/material";

export default function AssociationPage({}) {
  return (
    <Box component="main">
      <Typography variant="title" >Association Page</Typography>
      <Typography variant="smallTitle" >Initial commit!</Typography>
    </Box>
  )
}