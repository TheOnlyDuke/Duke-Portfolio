import { Box, Typography } from "@mui/material";

export default function ProjectPage({}) {
  return (
    <Box component="main">
      <Typography variant="title" >Projects Page</Typography>
      <Typography variant="smallTitle" >Initial commit!</Typography>
    </Box>
  )
}